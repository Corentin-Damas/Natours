extends base
include _myReviewCard

block content
  section.section-header-sm
    h1.heading-primary.ma-bt-md.bg-billing My billings
  main.main
    .masonry-grid
        each booking in bookings
            div
                .card
                    div.bg-transparent 
                    img.billing-bg(src=`/img/tours/${booking.tour.imageCover}`, alt=`${booking.tour.name}`)
                    div.billing-info
                        h1 #{booking.tour.name}
                        p.card__text= booking.tour.summary
                        div 
                            h6 Price 
                            h6= booking.price


                    //- .review-header-img
                    //-     img.card__picture-img(src=`/img/tours/${booking.tour.imageCover}`, alt=`${booking.tour.name}`)
                    //- .card__details
                    //-     h4.card__sub-heading= booking.tour.name




                    //-     h4.card__sub-heading= `${review.tour.difficulty} ${review.tour.duration}-day tour`
                    //-     p.card__text= review.tour.summary
                    //-     .card__data
                    //-         svg.card__icon
                    //-             use(xlink:href='/img/icons.svg#icon-map-pin')
                    //-         span= review.tour.startLocation.description
                    //-     .card__data
                    //-         svg.card__icon
                    //-             use(xlink:href='/img/icons.svg#icon-calendar')
                    //-         span= review.tour.startDates[0].toLocaleString('en-us', {month: 'long', year:"numeric"})
                    //-     .card__data
                    //-         svg.card__icon
                    //-             use(xlink:href='/img/icons.svg#icon-flag')
                    //-         span=`${review.tour.locations.length} stops`
                    //-     .card__data
                    //-         svg.card__icon
                    //-             use(xlink:href='/img/icons.svg#icon-user')
                    //-         span= `${review.tour.maxGroupSize} people`
                    //- .card__footer
                    //-     p
                    //-         span.card__footer-value= `$${review.tour.price}`
                    //-         | 
                    //-         span.card__footer-text per person
                    //-     p.card__ratings
                    //-         span.card__footer-value= review.tour.ratingsAverage
                    //-         | 
                    //-         span.card__footer-text= `rating (${review.tour.ratingsQuantity})`
